These libraries are required to compile and run standalone tests.

However, when running inside a servlet container (e.g.: tomcat)
these files are NOT required since they are provided by the
container.

servlet-api.jar

    This file is copied from the tomcat installation.  We use 
    tomcat for both local development, as well as production.
